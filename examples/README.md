# Examples

Runnable, self-contained examples. Each subfolder runs with a single command
and shows one clear use case.

- `basic/` — SQLite app + litestream sidecar via docker compose, backed by a
  local file replica.
