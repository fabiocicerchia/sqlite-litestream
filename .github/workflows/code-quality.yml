name: code-quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      # Lint the GitHub Actions workflows themselves.
      - name: actionlint
        uses: raven-actions/actionlint@3d39aea434753780c3b3d4a1a31c854b4dbf49d7  # v2.2.0

      # Enforce .editorconfig (indentation, EOL, trailing whitespace).
      - name: editorconfig-checker
        uses: editorconfig-checker/action-editorconfig-checker@51f63319f592f97930c73d9c46184d20bd206393  # v3.0.0
        with:
          # The action is pinned but the tool it downloads was not: it
          # resolved `latest`, and editorconfig-checker v4.0.0 renamed its
          # release assets from ec-* to editorconfig-checker-*. The action
          # looks for `ec-linux-amd64*`, so every run started failing with
          # "The binary 'ec-linux-amd64*' not found". v4 is a deliberate
          # bump, not something a release should make for us.
          version: v3.11.3

      # The action above only downloads the binary and adds it to PATH;
      # without this step nothing is ever checked.
      - name: editorconfig
        run: editorconfig-checker

      # Catch common typos in code and docs.
      - name: typos
        uses: crate-ci/typos@d43b6c087ac471e2ea7b8af622ff15f05c0c365b  # v1.50.1

      # pre-commit runs the hooks defined in .pre-commit-config.yaml.
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.x"
      - name: pre-commit
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd  # v3.0.1

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      # Deliberately not `make lint`: that verb is the whole pre-commit gate
      # (FC-GEN-057), which the job above already runs. This one stays the fast
      # subset — the checks that are about this image.
      - name: Dockerfile and shell
        run: |
          docker run --rm -i hadolint/hadolint < Dockerfile
          shellcheck entrypoint.sh test.sh

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - name: Build image and run smoke tests
        run: make test IMAGE=sqlite-litestream-ci
