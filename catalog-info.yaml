# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: sqlite-litestream
  description: >-
      Docker image that runs SQLite + Litestream side-by-side, replicating your database to S3-compatible
      storage in real time. Zero-ops SQLite in production.
  annotations:
    github.com/project-slug: fabiocicerchia/sqlite-litestream
    backstage.io/source-location: url:https://github.com/fabiocicerchia/sqlite-litestream/
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fabiocicerchia/sqlite-litestream
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
